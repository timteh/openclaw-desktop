{
  "name": "openclaw-desktop",
  "version": "2.0.0",
  "description": "OpenClaw Desktop — Tauri Gateway Chat Client",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win": "tauri build --target x86_64-pc-windows-msvc"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "marked": "^15.0.0",
    "highlight.js": "^11.11.0"
  }
}
